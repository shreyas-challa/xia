# Recursive Fibonacci — exercises functions, recursion and control flow.

fn fib(n: int) -> int:
    if n < 2:
        return n
    return fib(n - 1) + fib(n - 2)

fn main() -> int:
    let i = 0
    while i <= 10:
        print(fib(i))
        i = i + 1
    return 0
